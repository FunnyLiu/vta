{
  "version": "1.0.6",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "bootstrap": {
      "npmClientArgs": ["--silent", "--force"]
    },
    "publish": {
      "ignoreChanges": ["**/__tests__/**/*.*", "**/*.*ignore", "**/*.md"],
      "message": "refactor(release): release %v"
    }
  }
}
